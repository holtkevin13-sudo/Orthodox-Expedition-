ROW_COUNT=$(jq 'length' response.json)
          echo "Rows returned: ${ROW_COUNT}"

          if [ "${ROW_COUNT}" -lt 1 ]; then
            echo "::error::Reached Supabase but topics returned 0 rows."
            exit 1
          fi

          echo "✅ Supabase keep-alive ping succeeded."
